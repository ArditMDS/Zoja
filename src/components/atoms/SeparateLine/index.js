import React from "react";
import {CustomSeparateLine} from "./style";

function SeparateLine({color, width}) {
    return (
        <CustomSeparateLine color={color} width={width}/>
    )
}

export default SeparateLine